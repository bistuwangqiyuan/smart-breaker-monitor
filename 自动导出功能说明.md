# 智能断路器监控系统 - 自动导出功能说明

## 功能概述

现在上位机已经支持**自动导出Excel文件**功能！当系统接收到串口数据时，会自动将数据导出为Excel文件并保存在本地 `exports` 文件夹中。

## 主要特性

### ✅ 已实现的功能

1. **自动导出**: 接收到串口数据时自动导出Excel文件
2. **自定义文件名**: 支持用户自定义文件名模板
3. **灵活配置**: 可以开启/关闭自动导出功能
4. **实时通知**: 通过WebSocket实时通知导出状态
5. **错误处理**: 完善的错误处理和日志记录

## 使用方法

### 1. 基本使用（默认配置）

- 系统启动后，**自动导出功能默认开启**
- 默认文件名格式: `data_{设备地址}_{时间戳}.xlsx`
- 文件保存位置: `exports` 文件夹

### 2. 自定义文件名模板

#### 可用变量：
- `{deviceAddr}` - 设备地址
- `{deviceType}` - 设备类型  
- `{timestamp}` - 完整时间戳 (ISO格式)
- `{date}` - 日期 (YYYY-MM-DD)
- `{time}` - 时间 (HH-MM-SS)

#### 示例模板：
```
监测数据_{deviceAddr}_{date}_{time}
设备{deviceAddr}_电流电压数据_{timestamp}  
{date}_设备{deviceAddr}_监测报告
```

### 3. API接口使用

#### 开启/关闭自动导出
```http
POST /api/auto-export/enabled
Content-Type: application/json

{
  "enabled": true  // true开启，false关闭
}
```

#### 设置自定义文件名模板
```http
POST /api/auto-export/filename-template
Content-Type: application/json

{
  "template": "监测数据_{deviceAddr}_{date}_{time}"
}
```

#### 获取当前配置
```http
GET /api/auto-export/enabled        // 获取开关状态
GET /api/auto-export/filename-template  // 获取文件名模板
```

#### 手动导出（支持自定义文件名）
```http
POST /api/export-custom
Content-Type: application/json

{
  "deviceAddr": 1,
  "customFileName": "我的自定义文件名"  // 可选
}
```

### 4. 使用前端界面（如果有的话）

如果你有Web前端界面，可以在设置页面中：

1. **开关控制**: 添加一个开关按钮控制自动导出
2. **文件名设置**: 添加输入框设置文件名模板
3. **状态显示**: 显示导出成功/失败的实时状态

## 文件名示例

### 使用默认模板 `data_{deviceAddr}_{timestamp}`：
- `data_1_2025-09-11T08-30-45.xlsx`
- `data_2_2025-09-11T08-31-12.xlsx`

### 使用自定义模板 `监测数据_{deviceAddr}_{date}_{time}`：
- `监测数据_1_2025-09-11_08-30-45.xlsx`
- `监测数据_2_2025-09-11_08-31-12.xlsx`

### 使用自定义模板 `{date}_设备{deviceAddr}_电流电压数据`：
- `2025-09-11_设备1_电流电压数据.xlsx`
- `2025-09-11_设备2_电流电压数据.xlsx`

## 系统状态监控

### 日志记录
- 自动导出的成功/失败信息会记录在 `export.log` 文件中
- 服务器主要日志记录在 `server.log` 文件中

### WebSocket实时通知
前端可以监听以下WebSocket消息：
- `autoExportSuccess` - 导出成功
- `autoExportError` - 导出失败

## 注意事项

1. **文件覆盖**: 如果文件名重复，新文件会覆盖旧文件
2. **磁盘空间**: 请定期清理旧的导出文件，避免占用过多磁盘空间
3. **权限问题**: 确保程序对 `exports` 文件夹有写入权限
4. **性能影响**: 频繁的数据接收可能导致大量文件生成

## 测试自动导出功能

1. 确保串口已连接
2. 检查自动导出是否开启：`GET /api/auto-export/enabled`
3. 发送测试数据或等待真实数据
4. 检查 `exports` 文件夹中是否生成了新的Excel文件
5. 查看日志确认导出状态

## 故障排除

### 常见问题：

1. **文件没有生成**
   - 检查自动导出是否开启
   - 检查是否有数据接收
   - 查看日志文件中的错误信息

2. **文件名不符合预期**
   - 检查文件名模板设置
   - 确认模板中的变量拼写正确

3. **导出失败**
   - 检查磁盘空间是否充足
   - 确认exports文件夹权限
   - 查看error日志获取详细错误信息

## 更新日志

- **2025-09-11**: 实现自动导出功能
  - 新增自动导出开关
  - 新增自定义文件名模板功能
  - 新增相关API接口
  - 完善错误处理和日志记录 