# 自定义Excel文件名功能使用说明

## 📋 功能概述

现在你的上位机系统支持**自定义Excel文件名**功能！你可以：

1. **每次接收数据自动导出1个Excel文件**
2. **前端输入框自定义文件名**（不含.xlsx后缀）
3. **自动导出使用你设置的文件名**

## 🎯 核心功能

### ✅ 自动导出规律
- **每次接收到串口数据** → **自动导出1个Excel文件**
- **文件命名规则**：
  - 有自定义名称：`你的自定义名称_时间戳.xlsx`
  - 无自定义名称：`data_设备地址_时间戳.xlsx`（默认格式）

### ✅ 前端界面
- **输入框**：设置自定义文件名前缀
- **实时预览**：显示最终文件名效果
- **保存按钮**：保存设置到后端
- **状态显示**：显示当前使用的命名规则

## 🖥️ 前端使用方法

### 1. 设置自定义文件名

1. 在**数据操作**区域找到**"自定义文件名（不含后缀）"**输入框
2. 输入你想要的文件名，例如：
   - `监测报告`
   - `电流电压数据`
   - `设备测试结果`
   - `实验数据_第一组`

3. 点击**"保存"**按钮
4. 系统会显示："文件名前缀已设置为: 你的名称"

### 2. 预览文件名效果

- **有自定义名称时**：显示 `监测报告_时间戳.xlsx`
- **无自定义名称时**：显示 `data_设备地址_时间戳.xlsx (默认格式)`

### 3. 导出数据

- 点击**"导出数据"**按钮进行手动导出
- **自动导出**：接收串口数据时自动使用你设置的文件名

## 📝 文件名示例

### 自定义文件名：`监测报告`
- `监测报告_2025-09-11T08-30-45.xlsx`
- `监测报告_2025-09-11T08-31-12.xlsx`

### 自定义文件名：`设备1_电流测试`
- `设备1_电流测试_2025-09-11T08-30-45.xlsx`
- `设备1_电流测试_2025-09-11T08-31-12.xlsx`

### 默认格式（未设置自定义名称）
- `data_1_2025-09-11T08-30-45.xlsx`
- `data_255_2025-09-11T08-31-12.xlsx`

## 🔧 API接口说明

### 设置自定义文件名
```http
POST /api/auto-export/user-filename
Content-Type: application/json

{
  "fileName": "监测报告"
}
```

### 获取当前设置
```http
GET /api/auto-export/user-filename
```

响应：
```json
{
  "fileName": "监测报告",
  "isDefault": false,
  "previewName": "监测报告_时间戳.xlsx"
}
```

### 手动导出（使用自定义文件名）
```http
POST /api/export-custom
Content-Type: application/json

{
  "deviceAddr": 1,
  "customFileName": "临时文件名"  // 可选，覆盖设置的默认名称
}
```

## ⚡ 测试功能

### 1. 启动系统
```bash
# 启动系统
node server.js
# 或使用你的启动脚本
```

### 2. 打开前端界面
访问：`http://localhost:5173` 或你的前端地址

### 3. 设置自定义文件名
1. 在"数据操作"区域找到输入框
2. 输入：`测试数据`
3. 点击"保存"按钮

### 4. 测试导出
1. **手动导出**：点击"导出数据"按钮
2. **自动导出**：连接串口设备，等待数据接收

### 5. 检查结果
查看 `exports` 文件夹，应该看到：
```
测试数据_2025-09-11T15-30-45.xlsx
测试数据_2025-09-11T15-31-12.xlsx
```

## 📊 技术实现

### 后端修改
- **exportService.js**：添加用户自定义文件名支持
- **src/index.js**：添加文件名管理API接口
- **自动导出逻辑**：优先使用用户设置的文件名

### 前端修改
- **Home.vue**：添加自定义文件名输入框和管理功能
- **api.js**：添加文件名设置相关的API调用
- **实时预览**：显示最终文件名效果

## 🚀 高级功能

### 1. 清空自定义文件名
- 在输入框中删除所有内容
- 点击"保存"按钮
- 系统将恢复默认命名格式

### 2. 临时覆盖文件名
- 使用 `/api/export-custom` 接口
- 传入 `customFileName` 参数
- 仅对本次导出生效，不影响设置

### 3. 批量重命名
如果需要批量重命名已有文件，可以：
1. 进入 `exports` 文件夹
2. 使用批量重命名工具
3. 或编写脚本批量处理

## 💡 使用建议

### 1. 文件名规范
- 使用有意义的名称：`电压测试`、`故障检测`
- 避免特殊字符：`/\:*?"<>|`
- 长度适中：建议20字符以内

### 2. 时间管理
- 每次数据都会自动加时间戳
- 无需担心文件覆盖问题
- 可以按时间顺序查看数据

### 3. 存储管理
- 定期清理旧文件释放空间
- 重要数据及时备份
- 可以按项目分类管理

## 🎉 总结

现在你的系统具备了：
1. ✅ **每次接收数据自动导出1个Excel文件**
2. ✅ **前端输入框设置自定义文件名**
3. ✅ **自动导出使用你的自定义名称**
4. ✅ **实时预览文件名效果**
5. ✅ **保存设置到后端**

享受你的智能化监控系统吧！🚀 