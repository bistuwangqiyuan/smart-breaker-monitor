# 自定义文件名功能问题修复说明

## 🔍 问题分析

您遇到的两个问题：
1. **保存自定义文件名失败，显示404错误**
2. **没有自动按照输入的文件名命名并保存，还需要手动导出**

## 🎯 问题根源

### 问题1：404错误的原因
- **位置**: `src/index.js` 第686行
- **原因**: 通配符路由 `app.get('*', ...)` 放在了API路由之前
- **影响**: 拦截了所有请求，导致API路由无法正常访问

```javascript
// 错误的位置 - 在API路由之前
app.get('*', (req, res) => {  // ← 这里拦截了所有请求
  res.sendFile(path.join(__dirname, '..', 'public', 'index.html'));
});

// API路由放在后面就访问不到了
app.post('/api/auto-export/user-filename', ...);
```

### 问题2：自动导出逻辑本身是正确的
- **自动导出**: ✅ 已正确实现在 `src/index.js` 第711行
- **文件名生成**: ✅ 已正确实现在 `src/exportService.js` 第83-89行
- **问题**: 由于404错误，无法保存自定义文件名到服务器

## ✅ 修复方案

### 修复1：移动通配符路由位置

**修改文件**: `src/index.js`

**修改前**:
```javascript
// 前端路由处理
app.get('*', (req, res) => {
  res.sendFile(path.join(__dirname, '..', 'public', 'index.html'));
});

// 启动服务器
app.listen(PORT, () => {
  logger.info(`HTTP服务器启动在端口 ${PORT}`);
});
```

**修改后**:
```javascript
// 启动服务器
app.listen(PORT, () => {
  logger.info(`HTTP服务器启动在端口 ${PORT}`);
});

// 前端路由处理 - 必须放在最后，避免拦截API路由
app.get('*', (req, res) => {
  res.sendFile(path.join(__dirname, '..', 'public', 'index.html'));
});
```

### 修复2：确认自动导出逻辑（已经正确）

**自动导出流程**:
```javascript
// 1. 监听串口数据 (src/index.js 第700行)
serialService.on('data', async (data) => {
  // 2. 调用自动导出 (第711行)
  const result = await exportService.exportData(data.deviceAddr || 1, data);
});

// 3. exportData使用generateFileName (src/exportService.js 第197行)
fileName = this.generateFileName(realDeviceAddr, dataPacket);

// 4. generateFileName检查自定义文件名 (第85-89行)
if (this.userCustomFileName && this.userCustomFileName.trim() !== '') {
  const timestamp = new Date().toISOString().replace(/:/g, '-').replace(/\..+/, '');
  return `${this.userCustomFileName}_${timestamp}.xlsx`;
}
```

## 🚀 使用步骤（修复后）

### 1. 重启服务器（重要！）
```bash
# 停止当前服务器 (Ctrl+C)
# 然后重新启动
node server.js
```

### 2. 测试修复效果
```bash
node 测试修复.cjs
```

### 3. 在前端界面设置自定义文件名
1. 打开前端界面
2. 找到**"自定义文件名（不含后缀）"**的大输入框
3. 输入你想要的文件名，例如：`监测报告`、`实验数据`等
4. 点击**"保存文件名设置"**按钮
5. 看到成功提示：`文件名前缀已设置为: 你的名称`

### 4. 验证自动导出
1. 连接串口设备
2. 等待数据接收
3. 检查 `exports` 文件夹
4. 应该看到文件格式：`你的自定义名称_时间戳.xlsx`

## 📋 验证清单

### ✅ API功能验证
- [ ] GET `/api/auto-export/user-filename` 不再返回404
- [ ] POST `/api/auto-export/user-filename` 保存成功
- [ ] 前端"保存文件名设置"按钮正常工作

### ✅ 自动导出验证
- [ ] 串口接收数据后自动生成Excel文件
- [ ] 文件名使用自定义前缀：`你的名称_时间戳.xlsx`
- [ ] 文件保存在 `exports` 文件夹

### ✅ 数据真实性验证
- [ ] Excel文件包含100%真实串口数据
- [ ] 电流、电压、功率数据来自设备
- [ ] 时间戳为真实接收时间

## 🎯 预期效果

### 设置自定义文件名：`监测报告`
生成的Excel文件：
```
监测报告_2025-09-11T16-30-45.xlsx
监测报告_2025-09-11T16-31-12.xlsx
监测报告_2025-09-11T16-32-01.xlsx
```

### 设置自定义文件名：`实验数据_第一组`
生成的Excel文件：
```
实验数据_第一组_2025-09-11T16-30-45.xlsx
实验数据_第一组_2025-09-11T16-31-12.xlsx
```

## 🔧 故障排除

### 如果仍然出现404错误：
1. 确认服务器已重启
2. 检查控制台是否有错误信息
3. 运行测试脚本：`node 测试修复.cjs`

### 如果自动导出不使用自定义名称：
1. 确认在前端点击了"保存文件名设置"按钮
2. 检查是否有成功提示
3. 检查 `server.log` 中是否有保存日志

### 如果文件仍使用默认名称：
1. 检查自定义文件名是否真的保存成功
2. 运行：`node 测试修复.cjs` 验证
3. 查看日志文件确认自动导出调用

## 📞 技术支持

如果问题仍然存在，请提供以下信息：
1. 服务器控制台输出
2. 浏览器开发者工具的Network选项卡截图
3. `node 测试修复.cjs` 的运行结果
4. `exports` 文件夹中最新的文件名

**修复完成后，您的系统将完全按照需求工作：接收到数据后自动按照您设置的自定义文件名保存Excel文件！** 🎉 